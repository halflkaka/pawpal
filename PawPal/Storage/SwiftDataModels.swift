// SwiftData models removed — Supabase is now the single source of truth.
// StoredPost was retired in Phase 1 when real post creation and feed were
// wired to the `posts` and `post_images` Supabase tables.
